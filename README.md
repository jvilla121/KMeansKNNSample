# KMeansKNNSample
Using K-Nearest Neighbors ML approach on bank customer survey data in an attempt to predict whether or not customers will enroll in a new product offering. Also implementing K-Means ML approach in order to group students into clusters.
